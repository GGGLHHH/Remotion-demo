{
  "name": "remotion-editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -F editor dev",
    "build": "pnpm --filter \"@gedatou/*\" build",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "pnpm -r --parallel test",
    "release": "bumpp --no-verify"
  },
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "bumpp": "^11.0.1",
    "playwright": "^1.61.1"
  }
}
